{
  "name": "phaser3reactpractice",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "morgan": "^1.9.0",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "engines": {
    "node": "9.9.0"
  },
  "proxy": "http://localhost:3001",
  "scripts": {
    "start": "react-scripts start & nodemon --watch server/ server/bin/www",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
